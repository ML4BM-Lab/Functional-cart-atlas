# Python 3.12 runtime used only by reticulate in the Rocinante R image.
# anndata remains at the recovered workstation version; compiled dependencies
# use conservative Python 3.12-compatible releases. six 1.16.0 is the minimum
# tested release whose six.moves importer works with this embedded interpreter.

anndata==0.9.2
h5py==3.10.0
natsort==8.4.0
numpy==1.26.4
packaging==24.0
pandas==2.1.4
python-dateutil==2.8.2
pytz==2024.1
scipy==1.11.4
six==1.16.0
tzdata==2024.1
